{
  "name": "cc-quick",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:fetch": "node scripts/fetch-upstream.mjs",
    "sync:parse": "node scripts/parse-upstream.mjs",
    "sync:transform": "node scripts/transform-content.mjs",
    "sync:build": "node scripts/build-page.mjs",
    "sync": "npm run sync:fetch && npm run sync:parse && npm run sync:transform && npm run sync:build"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
